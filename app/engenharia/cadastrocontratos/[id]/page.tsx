import CadastroContrato from '../page'

// This is the edit page - it reuses the same component
// The parent page.tsx checks for params.id to determine if it's edit mode
export default CadastroContrato
